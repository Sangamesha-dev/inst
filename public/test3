Write a short program that prints each number from 1 to 100 on a new line. For each multiple of 3, 
print "Holly" instead of the number. 
For each multiple of 5, print "Molly" instead of the number. 
For numbers which are multiples of both 3 and 5, print "HollyMolly" 
instead of the number.